name: decoration
description: Code from the "Paint your own Decoration" presentation.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:

flutter:
  uses-material-design: true
